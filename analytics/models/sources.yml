sources:
  - name: jobs
    database: raw
    schema: jobs
    tables:
      - name: raw_job_postings
        description: One row per posting per scrape event.
      - name: raw_job_skills
        description: One row per (job, skill) pair extracted from the postings.